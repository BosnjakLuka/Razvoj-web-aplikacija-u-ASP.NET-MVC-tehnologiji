@model IReadOnlyList<planinarenje.Models.PodrucjeIndexCardViewModel>

@section Breadcrumbs {
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb mb-0">
            <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Naslovnica</a></li>
            <li class="breadcrumb-item active" aria-current="page">Područja</li>
        </ol>
    </nav>
}

<section class="mb-4">
    <div class="d-flex flex-wrap justify-content-between align-items-start gap-3">
        <div>
            <h1 class="section-title no-accent mb-2">Planinarska područja Hrvatske</h1>
            <p class="text-muted mb-0">Pregled područja obilaznice, uz brzu pretragu i CRUD akcije.</p>
        </div>
        <a asp-action="Create" class="btn btn-primary">Novo podrucje</a>
    </div>
</section>

<div class="app-card mb-4">
    <input type="text" id="searchInput" class="form-control" placeholder="Pretrazi podrucja..." />
</div>

<div id="resultsContainer">
    @await Html.PartialAsync("_PodrucjeListPartial", Model)
</div>

@section Scripts {
    <script>
        $(function () {
            $('#searchInput').on('input', function () {
                const term = $(this).val();
                $.ajax({
                    url: '@Url.Action("Search", "Podrucje")',
                    data: { searchTerm: term },
                    success: function (html) {
                        $('#resultsContainer').fadeOut(150, function () {
                            $(this).html(html).fadeIn(200);
                        });
                    }
                });
            });
        });
    </script>
}
